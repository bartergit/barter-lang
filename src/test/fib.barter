dec_func('fib', 'int',
    block(
        set_arg('x', 'int')
    ),
    block(
        cond(lt(x,2), block(
            ret(1)
        )),
        ret(sum(fib(dif(x,1)),fib(dif(x,2))))
    )
);
dec_func('main', 'void',
    block(),
    block(
        cout(fib(8))
    )
);
