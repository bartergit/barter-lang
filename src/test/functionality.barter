dec_func('do_some', 'int',
    block(
        set_arg('x', 'int'),
        set_arg('y', 'int')
    ),
    block(
        ret(8)
    )
);
dec_func('do_some2', 'int',
    block(
        set_arg('x', 'int')
    ),
    block(
        ret(x)
    )
);
dec_func('return_bool', 'bool',
    block(),
    block(
        ret(true)
    )
);
dec_func('do_void', 'void',
    block(set_arg('x','int')),
    block(
        dec_var('y', 'bool', true),
        cout('doing nothing really. and x is', x, y),
        ret_void()
    )
);
dec_func('main', 'void', //hello;
    block(),
    block(
        dec_var('a', 'int', sum(3,2)),
        dec_var('a', 'bool', true)
    )
);